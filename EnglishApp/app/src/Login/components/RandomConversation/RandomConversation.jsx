import { View, StyleSheet, Text } from "react-native";

export default function RandomConversation() {
  return (
    <View>
        <Text>
            Here its where the random cube are gonna pick a random conversation on BD
        </Text>
    </View>
  );
};

const styles = StyleSheet.create({

});
